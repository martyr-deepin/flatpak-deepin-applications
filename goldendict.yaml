---
app-id: org.deepin.flatdeb.goldendict
branch: master
runtime: org.deepin.flatdeb.kf5.Platform
runtime-version: unstable
sdk: org.deepin.flatdeb.kf5.Sdk
command: goldendict
writable-sdk: true
appstream-compose: false
modules:
  - name: goldendict
    x-flatdeb-apt-packages:
      - goldendict
    sources:
      - type: file
        path: deb-buildapi/configure
        dest: '.'
      - type: file
        path: goldendict/Makefile

finish-args:
  - --env=LD_LIBRARY_PATH=/app/lib/x86_64-linux-gnu:/app/lib
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
  - --env=DCONF_USER_CONFIG_DIR=.config/dconf
  - --filesystem=xdg-run/dconf
  - --filesystem=~/.config/dconf:ro
  - --talk-name=ca.desrt.dconf
  - --env=SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
rename-desktop-file: goldendict.desktop
copy-icon: false
...
