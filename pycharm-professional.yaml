---
app-id: org.deepin.flatdeb.pycharm-professional
branch: master
runtime: org.deepin.flatdeb.gnome.Platform
runtime-version: unstable
sdk: org.deepin.flatdeb.gnome.Sdk
command: pycharm-professional
writable-sdk: true
appstream-compose: false
modules:
  - name: pycharm-professional
    x-flatdeb-apt-packages:
      - pycharm-professional
    sources:
      - type: file
        path: deb-buildapi/configure
        dest: '.'
      - type: file
        path: deb-buildapi/Makefile
    build-options:
      no-debuginfo: true
      strip: false

finish-args:
  - --env=LD_LIBRARY_PATH=/app/lib/x86_64-linux-gnu:/app/lib
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
  - --env=DCONF_USER_CONFIG_DIR=.config/dconf
  - --filesystem=xdg-run/dconf
  - --filesystem=~/.config/dconf:ro
  - --talk-name=ca.desrt.dconf
  - --env=SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
cleanup-commands:
  - rm -f /app/bin/pycharm-professional
  - ln -sf /app/lib/pycharm-professional/bin/pycharm.sh /app/bin/pycharm-professional
  - install -Dm644 /app/lib/pycharm-professional/bin/pycharm.png /app/share/icons/hicolor/128x128/apps/pycharm-professional.png
rename-icon: pycharm-professional
rename-desktop-file: jetbrains-pycharm.desktop
copy-icon: true
...
