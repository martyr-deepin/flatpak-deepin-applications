---
app-id: org.deepin.flatdeb.gweled
branch: master
runtime: org.deepin.flatdeb.gnome.Platform
runtime-version: unstable
sdk: org.deepin.flatdeb.gnome.Sdk
command: gweled
writable-sdk: true
appstream-compose: false
modules:
  - name: gweled
    x-flatdeb-apt-packages:
      - gweled
    sources:
      - type: file
        path: deb-buildapi/configure
        dest: '.'
      - type: file
        path: gweled/app.tar.xz
        dest: '.'
      - type: file
        path: gweled/Makefile

finish-args:
  - --env=LD_LIBRARY_PATH=/app/lib/x86_64-linux-gnu:/app/lib
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
  - --device=all
  - --env=DCONF_USER_CONFIG_DIR=.config/dconf
  - --filesystem=xdg-run/dconf
  - --filesystem=~/.config/dconf:ro
  - --talk-name=ca.desrt.dconf
  - --env=SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
rename-desktop-file: gweled.desktop
copy-icon: false
...
