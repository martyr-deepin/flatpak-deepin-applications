---
app-id: org.deepin.flatdeb.teamviewer-host
branch: master
runtime: org.deepin.flatdeb.Base.Platform
runtime-version: unstable
sdk: org.deepin.flatdeb.Base.Sdk
command: teamviewer-host
writable-sdk: true
appstream-compose: false
separate-locales: false
modules:
  - name: teamviewer-host
    x-flatdeb-apt-packages:
      - teamviewer-host
    sources:
      - type: file
        path: deb-buildapi/configure
        dest: '.'
      - type: file
        path: deb-buildapi/Makefile
    post-install:
      - install -Dm644 /app/opt/teamviewer/tv_bin/desktop/com.teamviewer.TeamViewer-Host.desktop  /app/share/applications/teamviewer-host.desktop
      - install -Dm644 /app/opt/teamviewer/tv_bin/desktop/teamviewer.png /app/share/icons/hicolor/48x48/apps/teamviewer-host.png
      - sed -i 's|Exec=.*|Exec=teamviewer-host|g' /app/share/applications/teamviewer-host.desktop
finish-args:
  - --env=LD_LIBRARY_PATH=/app/lib/x86_64-linux-gnu:/app/lib
  - --env=SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
  - --env=DCONF_USER_CONFIG_DIR=.config/dconf
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
  - --talk-name=ca.desrt.dconf
  - --talk-name=org.freedesktop.FileManager1
  - --talk-name=org.freedesktop.portal.Fcitx
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=com.deepin.filemanager.filedialog
  - --filesystem=xdg-run/dconf
  - --filesystem=~/.config/dconf:ro
  - --filesystem=home
cleanup-commands:
  - echo "#!/bin/sh\nLD_PRELOAD=/usr/lib/x86_64-linux-gnu/libsnappypreload.so /app/opt/teamviewer/tv_bin/script/teamviewer \$@\n" | tee /app/bin/teamviewer-host
  - chmod +x /app/bin/teamviewer-host
rename-icon: teamviewer-host
rename-desktop-file: teamviewer-host.desktop
copy-icon: true
...
